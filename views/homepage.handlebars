<div class="homepage-posts">
  {{#each posts as |post|}}
    <div class="post">
      <div class="post-header">
        <h2>{{post.title}}</h2>
        <p>Post by user id {{post.user_id}}</p>
      </div>
      <p>{{post.content}}</p>
    </div>
  {{/each}}
</div>